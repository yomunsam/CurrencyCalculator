# 开发约定

- 注释使用 Mandarin（中文普通话）；标识符沿用英文，界面文案走现有国际化机制。
- 按职责组织目录与文件，避免大量文件平铺、巨型文件或把所有交互塞进 Home。拆分应有明确用途。
- 保持消融实验意识：增加抽象、依赖或防御分支前，先考虑去掉它是否仍能解决问题。优先直接实现，不为假想需求提前封装；保留网络、存储、用户输入等真实边界的必要处理。
- 代码以可读为先，不将复杂逻辑压缩成一行；注释解释原因，避免复述代码。

## 项目边界

- .NET 10 / Blazor WebAssembly PWA，纯静态部署，无后端。运行和发布见 README。
- 币种统一定义在 `Core/CurrencyCatalog.cs`；新增币种同时补齐本地图标、三语名称、fallback 脚本与数据，见 `Docs/新增货币类型指南.md`。
- 汇率快照以 USD 为基准；保留离线可用性、数据来源和过期提示，不能将缺失汇率当作有效汇率。
- 现有界面支持英文、简体中文、日文，主题使用 CSS 变量。保留用户偏好与排序。
- 交互按实际触控、鼠标、键盘事件处理，布局按可用空间适配，不将屏幕尺寸等同于输入方式。
- `Docs/` 中初始需求、里程碑和 ADR 是历史背景；以当前用户要求和实际代码为准，不将旧计划当成新增约束。
